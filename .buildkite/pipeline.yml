steps:
  - label: 'Ruby'
    plugins:
      - docker-compose#v3.7.0:
          run: ruby
          env:
            - recent=2
    artifact_paths:
      - build/*.txt
      - reports/*.html
  - label: 'PHP'
    plugins:
      - docker-compose#v3.7.0:
          run: php
          env:
            - recent=2
    artifact_paths:
      - build/*.txt
      - reports/*.html
  - label: 'JS'
    plugins:
      - docker-compose#v3.7.0:
          run: js
          env:
            - recent=2
    artifact_paths:
      - build/*.txt
      - reports/*.html
  - label: 'Java'
    plugins:
      - docker-compose#v3.7.0:
          run: java
          env:
            - recent=2
    artifact_paths:
      - build/*.txt
      - reports/*.html
  - label: 'Python'
    plugins:
      - docker-compose#v3.7.0:
          run: python
          env:
            - recent=2
    artifact_paths:
      - build/*.txt
      - reports/*.html